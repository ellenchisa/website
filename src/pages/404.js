import React from "react"

import Layout from "../components/layout"

const NotFoundPage = () => (
  <Layout>
    <h1>Not found :(</h1>
    <p>Nope, doesn't exist, sorry!</p>
  </Layout>
)

export default NotFoundPage
